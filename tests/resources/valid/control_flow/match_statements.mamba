match a with
    b => c
    d => e

match b, bb, bbb with
    (a, c) => print "hello world"

match nested with
    a => b
    c => match c with
        other => even_other
        other_one => better_one
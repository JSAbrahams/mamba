from Math use abs

util my_class
    def SOME_CONSTANT <- 3.5E20

type some_state isa my_class when z_modified > 2

type other_state isa my_class when
    z_modified > 10
    z_modified < 200
    other_field < 50

class my_class
    def private z_modified
    def private other_field <- 10

    def init(def my_field: Int, z: Int) ->
        z_modified <- z * SOME_CONSTANT

    def fun_a() -> print self

    def factorial(0) -> 1
    def factorial(x: Int) -> x * factorial x - 1

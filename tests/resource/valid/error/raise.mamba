class Err(msg: Str): Exception(msg)

def f(x: Int) -> Int ! Err := if x > 0 then return 10 else ! Err("Expected positive number.")

def g() -> Int ! Err := ! Err("Error always raised")

def h(x: Int) -> Int ! Err := if x < 0 then ! Err("Less than") else ! Err("Greater Than")

f(10)

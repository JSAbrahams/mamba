# A field declared in the body is derived from the class arguments, so it need not be
# passed at the construction site
class Circle(radius: Int) where
    def diameter: Int := self.radius * 2

    def pure describe(self) -> Int := return self.diameter
end

def c := Circle.new(2)
print(c.diameter)
print(c.describe())

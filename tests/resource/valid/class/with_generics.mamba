class InnerClass[A] where
    def inner(a: A) -> A := a
end
class OuterClass[B] where
    def inner_class: InnerClass[B]
end

def outer_class := OuterClass()
print(outer_class.inner_class.inner("hello world"))

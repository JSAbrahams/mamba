# `def pure new(..)` asserts that constructing this class is pure, which is what lets a pure
# function do it. Purity is never inferred, so without the assertion this would be rejected
class Point(x: Int, y: Int) where
    def pure new(..)
end

def pure origin() -> Point := return Point.new(0, 0)

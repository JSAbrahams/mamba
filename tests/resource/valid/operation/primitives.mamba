def aa := Complex.new(10, 20)
def ab := Complex.new(10, 20.0)
def ac := Complex.new(10.0, 20)
def ad := Complex.new(10.0, 20.0)

def b := Int.new(10)
def c := Int.new("10")
def d := Int.new(10.2)

def e := Float.new(10)
def f := Float.new(2.3)
def g := Float.new("30")

def h := Bool.new(True)

def i := Str.new(10)
def j := Str.new("hello world")
def k := Str.new(20.0)
def l := Str.new(Complex.new(3, 1))
def m := Str.new(True)

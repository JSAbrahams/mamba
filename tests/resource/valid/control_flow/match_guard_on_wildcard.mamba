# A guard may sit on a wildcard, which binds nothing but still matches
def threshold := 5

def pure over(x: Int) -> Str := match x where
    _ if x > threshold => "over"
    _                  => "under"
end

print(over(10))
print(over(1))

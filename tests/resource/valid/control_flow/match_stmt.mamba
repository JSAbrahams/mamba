def a := "d"

def (b, bb, bbb) := (0, 1, 2)

match (b, bb, bbb)
    (0, 1, 2) => print("hello world")

def nested := "other"

match nested
    "a" =>
        "b"
        "c"
    "c" => match nested
        "other" => "even_other"
        "other_one" => "better_one"
        _ => "default"
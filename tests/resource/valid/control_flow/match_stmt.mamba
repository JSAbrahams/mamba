def a := "d"

def (b, bb, bbb) := (0, 1, 2)

match (b, bb, bbb) with
    (0, 1, 2) => print("hello world")
end

def nested := "other"

match nested with
    "a" => do
        "b"
        "c"
    end
    "c" => match nested with
        "other" => "even_other"
        "other_one" => "better_one"
        _ => "default"
    end
end

status:
  # changes tells us if there are unexpected code coverage changes in other files
  # which were not changed by the diff
  changes: yes

coverage:
  range: "50...100"

comment:
  layout: "header, diff, sunburst, files"
